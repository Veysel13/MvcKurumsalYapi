@model Northwind.Entities.Cart

<div>
    <span style="color: white" class="caption">
        <b>Your cart(@Model.Lines.Sum(x=>x.Quantity))</b>
        @Model.Total.ToString("c")
    </span>
    @Html.ActionLink("Checkout","Index","Cart")
</div>
